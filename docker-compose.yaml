version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: my-postgres
    command: -p 5324
    ports:
      - "5324:5324"
    expose:
      - "5324"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: timeshare
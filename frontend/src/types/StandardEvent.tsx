
type StandardEvent = {
  start: Date; // event start
  end: Date;   // inclusive
  title: string;
  extraClasses: string;
  left: number;
  width: number;
  top: number;
};

export type { StandardEvent };